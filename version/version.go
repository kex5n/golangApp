package version

var  (
	// BuildTime is a time label of the moment when the binary was buult
	BuildTime = "unset"
	// Commit is a last commit has at the moment when the binary was built
	Commit = " unset"
	// Release is a semantic version of current build
	Release = "unset"
)